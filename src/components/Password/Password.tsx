import Input from "../Input/Input";
import { PasswordWrapper } from "./Password.styled";

const Password = () => {
  return (
    <PasswordWrapper>
      {/* <Input type="password" />
      <Input type="text" /> */}
    </PasswordWrapper>
  );
};

export default Password;
